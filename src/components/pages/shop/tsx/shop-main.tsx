import React from "react";
import{ShopCard} from './card'

export const ShopMain:React.FC = () =>{
    return (
        <section className="produce-subtitle">
            <ShopCard/>
            
        </section>
    );
}