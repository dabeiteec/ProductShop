import { useState,useEffect,useRef } from 'react'
import {Layout} from './Layout'

const App:React.FC=()=> {
  const [login, setLogin] = useState('');

  return (
   <div>
    <Layout>
      
    </Layout>
   </div>
  )
}

export  {App}
